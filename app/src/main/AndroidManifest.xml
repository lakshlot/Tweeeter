<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lakshay.tweeeter">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/tweeetterlogo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/CustomTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="f83dfef963c764b48d56cd7d5d421c8590eebb46" />

        <activity android:name=".Search_Activity"></activity>
        <activity
            android:name=".User_Profile_Activity"
            android:label="@string/title_activity_user__profile_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".OAuthRequestActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/CustomTheme"></activity>
    </application>

</manifest>